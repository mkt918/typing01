# プロジェクト仕様書：Sushi Typer Factory (仮)

## 1. プロジェクト概要

* **タイトル:** Sushi Typer Factory（仮称）
* **コンセプト:** 「タイピングゲーム」×「インクリメンタルゲーム（放置系）」の融合。
* **ターゲット:** 中学生（射幸心、インフレ数字、ガチャ要素を重視）。
* **プラットフォーム:** Webブラウザ（PC/SP両対応）。
* **技術スタック:** HTML5, CSS3, Vanilla JavaScript (ES6+)。外部ライブラリは極力使用せず、軽量化を徹底する。

## 2. コアゲームループ

1. **Active (Typing):** プレイヤーは単語をタイプし、即時の「現金」と「電力（Energy）」を獲得する。
2. **Passive (Management):** 獲得した現金で「ガチャ」を回し、寿司生産設備（Emoji）を入手・強化する。
3. **Synergy:** 「電力」が高いほど、設備の生産効率（自動収益）が倍増する。電力が尽きると生産が激減するため、プレイヤーは定期的にタイプする必要がある。
4. **Inflation:** 数値は「万・億・兆・京…無量大数」までインフレし、成長実感を与える。

## 3. データ構造とステート管理

### 3.1 グローバルステート (`gameState`)

全ての数値はブラウザの `localStorage` に保存・復元可能とする。

| 変数名 | 型 | 説明 |
| --- | --- | --- |
| `money` | **BigInt** | 現在の所持金（上限なし、インフレ対応）。 |
| `energy` | Float | 工場の電力。0.0〜100.0。時間経過で減衰。 |
| `totalProduction` | BigInt | 設備による毎秒の自動生産額（基礎値）。 |
| `clickValue` | BigInt | タイピング1回あたりの獲得金額。 |
| `inventory` | Array | 入手した設備（ガチャ結果）のリスト。 |
| `highScore` | Number | 最大WPM（Words Per Minute）記録。 |

### 3.2 設備オブジェクト（Inventory Item）

ガチャで排出されるオブジェクト。

```javascript
{
  id: "item_001",
  name: "タワシロボ",
  emoji: "🤖",
  rarity: "N", // N, R, SR, SSR, UR
  production: 100n, // BigInt: 秒間生産額
  multiplier: 1.0   // 固有倍率
}
```

## 4. 機能要件詳細

### 4.1 タイピングエンジン

* **ワードリスト:** 3段階の難易度を用意。
  * Level 1: ネットスラング（w, kusa, nida）
  * Level 2: 中二病ワード（chaos, dragon, awakening）
  * Level 3: 意識高い系（consensus, synergy）

* **入力判定:** ローマ字入力に対応。「し(si/shi)」「ち(ti/chi)」「ん(nn/n)」などの揺らぎ許容ロジックを実装。
* **フィードバック:**
  * 成功時：パーティクル演出 + お金増加 + 電力回復（+5%）。
  * コンボボーナス：ミスなし10回で「Fever Mode」（背景明滅、取得金額2倍）。

### 4.2 電力（Energy）システム

* **減衰:** 毎秒 `-2.0%` の速度で自然減少。
* **効率変動（Production Multiplier）:**
  * **Overdrive (80-100%):** 生産効率 **200%**
  * **Normal (20-79%):** 生産効率 **100%**
  * **Low Power (0-19%):** 生産効率 **10%**（ペナルティ）

* **UI表示:** CSSのプログレスバーで可視化。色を変化させる（青→緑→赤）。

### 4.3 経済・ガチャシステム

* **通貨単位:** 日本の単位表記関数 `formatMoney(BigInt)` を実装。
  * 例: `1234567890000` → "1兆2345億6789万円"

* **ガチャロジック:**
  * コスト：所持金に応じて上昇（現在の毎秒生産額の100倍など）。
  * 排出率：
    * N (50%): 生産力 低
    * R (30%): 生産力 中
    * SR (15%): 生産力 高
    * SSR (4%): 生産力 超高 + 特殊効果
    * UR (1%): 生産力 崩壊級（インフレ加速トリガー）

* **演出:** CSS `animation` を使用し、カプセル（🎁）が揺れて光る演出を入れる。

## 5. UI/UX デザイン要件（HTML/CSS）

外部画像は使わず、全てCSSとEmojiで構成する。

* **テーマカラー:**
  * 背景: ダークネイビー (`#1a1a2e`)
  * アクセント: ネオンピンク (`#e94560`) と サイバーグリーン (`#0f3460`)

* **レイアウト:**
  * **Header:** 所持金（特大フォント）、電力バー。
  * **Main:** タイピング表示エリア。寿司ネタEmoji (`🍣`) が中央に鎮座。
  * **Footer:** 「ガチャを回す」ボタン（巨大）、所持している設備リスト（スクロール可能）。

## 6. 実装ステップ（Claude Codeへの指示順序）

1. **Step 1: 基盤構築**
   * `index.html`, `style.css`, `script.js` の作成。
   * BigInt対応の数値フォーマット関数 `formatMoney` の実装。
   * 基本的なゲームループ（`requestAnimationFrame`）の実装。

2. **Step 2: タイピング機能の実装**
   * 単語データの定義。
   * キー入力イベントの取得と判定ロジック。
   * 正解時のマネー加算処理。

3. **Step 3: 電力・自動生産の実装**
   * 電力減衰ロジック。
   * 電力残量に基づく生産倍率計算。
   * `setInterval` または ゲームループ内での自動加算処理。

4. **Step 4: ガチャ・インベントリの実装**
   * ガチャ確率計算ロジック。
   * 設備オブジェクトの生成とInventoryへの追加。
   * Inventoryの総生産額計算。

5. **Step 5: 保存・仕上げ**
   * `localStorage` へのSave/Load処理。
   * CSSアニメーション（Fever時、ガチャ時）の追加。
